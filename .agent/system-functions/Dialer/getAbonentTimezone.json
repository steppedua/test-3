{
  "description" : "Returns the timezone offset of the client as a formatted string (e.g. '+03:00:00'). Returns null if not available",
  "input" : {
    "type" : "object",
    "properties" : { },
    "required" : [ ]
  },
  "output" : {
    "type" : "string",
    "description" : "The formatted timezone offset (e.g. '+03:00:00') or null"
  }
}