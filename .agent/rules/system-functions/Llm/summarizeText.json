{
  "type" : "object",
  "properties" : {
    "text" : {
      "type" : "string",
      "description" : "The text to summarize"
    },
    "targetSize" : {
      "type" : "number",
      "description" : "Desired summary length in characters. If not specified, the LLM will determine the optimal length"
    },
    "lang" : {
      "type" : "string",
      "description" : "Summary language. Defaults to the source text language"
    },
    "combineStrategy" : {
      "type" : "string",
      "description" : "Strategy for combining chunk summaries: 'recursive' (recursive merging) or 'iterative' (sequential refinement, default)"
    }
  },
  "required" : [ "text" ]
}